/**
 * An interface to define the result of an expression (adding 2 numbers, dividing 2 numbers, etc.)
 * for lambda functions and anonymous classes
 */
public interface EvaluateExpressor {
    public double signMethod(double a, double b);
}
